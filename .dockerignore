.git/
.claude/
__pycache__/
**/__pycache__/
*.pyc
*.pyo
*.pyd

# Runtime data — mounted as volumes, not baked in
chroma_db/
data/

# Local model dir — baked in via builder stage, not copied from host
models/

# SQLite state files — recreated at runtime
*.sqlite
*.db

# Python virtual environment
env/
.venv/
venv/

# Build/dev artifacts
embedding_plots/
*.egg-info/
dist/
build/
.pytest_cache/

# Secrets — never bake into the image
.env
.env.local

# OS noise
.DS_Store
Thumbs.db

