# Copy this file to .env and fill in your values before running docker compose build.

# Hugging Face token (optional) — speeds up the embedding model download by bypassing
# anonymous rate limits. Get one at https://huggingface.co/settings/tokens (read-only is enough).
# Leave blank or remove this line to download anonymously.
HF_TOKEN=
